{
  "name": "sacrosoft-api-tests",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "test:api": "playwright test tests/api",
    "test:api:headed": "playwright test tests/api --headed",
    "test:api:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.0"
  }
}
